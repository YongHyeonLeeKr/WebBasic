// 데이터베이스를 사용해서 Board Entity를 사용하게 되어서 Board 모델은 따로 필요 없음 -> BoardStstus 만 따로 board-status.enum.ts 파일로 이관

// export interface Board {
//   id: string;
//   title: string;
//   description: string;
//   status: BoardStatus;
// }
//
// export enum BoardStatus {
//   PUBLIC = 'PUBLIC',
//   PRIVATE = 'PRIVATE',
// }
