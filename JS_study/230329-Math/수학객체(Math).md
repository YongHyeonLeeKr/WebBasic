# Math

> 자주쓰는 수학객체 정리 

``` js
Math.min(2,4,5) // 최솟값 2
Math.max(2,4,5) // 최댓값 5
Math.floor(2.124125) // 내림 2
Math.ceil(2.235236) // 올림  3 
Math.round(2.235236) // 반올림 2
Math.random() // 0~1까지 랜덤 0.40652478235637757(예시)
Math.floor(Math.random() * 1000000) //  여섯 자리 숫자 랜덤 생성 125162
```