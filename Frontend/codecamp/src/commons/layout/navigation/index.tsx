import styled from '@emotion/styled';

export default function LayoutNavigation (): JSX.Element {
    return <Wrapper>내비게이션 입니다.</Wrapper>;
}

const Wrapper = styled.div `
    height: 50px ;
    background-color: gray;
`